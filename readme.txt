

*Keras1.py=Genera un modelo de una red neuronal profunda y evalua su matriz de confución y el comportamiento de la precisión y el error respecto al entrenamiento (época)

*SVC.py= Es un borrador para generar y evaluar modelos basados en SVC

*Gui.py= Corre una interfaz gráfica que permite ingresar un dígito manualmente para ser evaluado por los modelos generados por keras.py y SVM.py.

Estos modelos son= numbers.h5  y los modelos que se encuentran en la carpeta SVM_models

